{
  "private": true,
  "scripts": {
    "build": "spago bundle-app --to dist/chordeditor.js",
    "test": "spago -x ./test.dhall test"
  },
  "devDependencies": {
    "big-integer": "^1.6.51",
    "esbuild": "^0.14.54",
    "purescript": "^0.15.10",
    "spago": "^0.21.0"
  }
}
