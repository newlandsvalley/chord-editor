{
  "private": true,
  "scripts": {
    "build": "pulp build --to dist/chordeditor.js",
    "optimise": "npm run uglify --s dist/chordeditor.js --output dist/chordeditormin.js --compress --mangle"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  },
  "dependencies": {
    "uglify": "^0.1.5",
    "uglify-js": "^3.5.11"
  }
}
