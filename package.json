{
  "private": true,
  "scripts": {
    "build": "spago bundle --outfile dist/chordeditor.js --bundle-type app --platform browser",
    "test": "spago test"
  },
  "devDependencies": {
    "big-integer": "^1.6.51",
    "esbuild": "^0.18.20",
    "purescript": "^0.15.15",
    "spago": "^0.93.42"
  }
}
