{
  "private": true,
  "scripts": {
    "build": "spago bundle-app --to dist/chordeditor.js",
    "test": "spago -x ./test.dhall test",
    "pulp build": "pulp build --to dist/chordeditor.js"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript": "^0.13.8",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
